package cc.crochethk.compilerbau.praktikum.ast.types;

public final class BoolT extends Type {
    public BoolT(int line, int column, String tokenText) {
        super(line, column, tokenText);
    }
}