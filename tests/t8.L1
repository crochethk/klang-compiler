/**
 * Tries out:
 *      - double support
 */
fn max(a: f64, b: f64): f64 {
    return a>=b ? a : b;
}

fn max3(a: f64, b: f64, c: f64): f64 {
    if max(a,b) >= max(b,c) {
        return max(a,b);
    } 
    // long for "max(b,c)"
    else {
        if b >=c {
            return b;
        }
        else {/*empty statement*/}
        /* NO empty statement */
    }
    if true {/*empty statement*/}
        /* NO empty statement */
    return c;
}

fn ___main___(): f64 {
    let MIN: f64 = 60.0;
    let MID: f64;
    MID = 50.0-1.f64+1f64;
    /*
    return max3(MID, MIN, 71.);
    return max3(MID,72.,MIN);
    return max3(73.,MID,MIN);
    return max3(74.,MIN,MID);
    */
    return max3(75.,MIN,MID);
    return max3(MIN,76.0,MID);
}