/**
 * Tries out:
 *     - funcall
 *     - boolean result
 *     - neg operator
 *     - comparison operator
 */
fn is_odd(number: int): boolean {
    // return number % 2 != 0;
    return !(number - (number/2) * 2 == 0);
}

fn ___main___(): boolean {
    return is_odd(2147483647);
}